metadata:
  requests:
    units: /min
    precision: 1
  errors_req:
    label: Errors (Req)
    units: /min
    precision: 1
  errors_conn:
    label: Errors (Conn)
    units: /min
    precision: 1
  errors_resp:
    label: Errors (Resp)
    units: /min
    precision: 1
  proxy_up:
    label: Proxy Up
    precision: 0
  bytes_in:
    label: Bytes In
    units: /sec
    precision: 1
  bytes_out:
    label: Bytes Out
    units: /sec
    precision: 1
  active_sessions:
    label: Active Sessions
    units: sessions
    precision: 0
  queued_sessions:
    label: Queued Sessions
    units: sessions
    precision: 0
  active_servers:
    label: Active Servers
    units: servers
    precision: 0