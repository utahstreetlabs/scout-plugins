metadata:
  database_reads:
    units: /sec
    precision: 1
  database_writes:
    units: /sec
    precision: 1
  requests:
    units: /sec
    precision: 1
  requests:
    label: HTTP requests
    units: /sec
    precision: 1
  view_reads:
    units: /sec
    precision: 1
  httpd_success:
    label: HTTP requests (success)
    units: /sec
    precision: 1
  httpd_error:
    label: HTTP requests (error)
    units: /sec
    precision: 1
  GET:
    units: /sec
    precision: 1
  POST:
    units: /sec
    precision: 1
  PUT:
    units: /sec
    precision: 1
  DELETE:
    units: /sec
    precision: 1