options:
  mysql_slow_log:
    default: /var/log/mysql/mysql-slow.log
    name: Full path to the MySQL slow queries log file
  max_query_time:
    default: 3
    notes: Alert if the query goes longer than this time in seconds.